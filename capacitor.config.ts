import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pohang-domitory-meal',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
